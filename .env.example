# 课表管理平台环境变量配置
# 复制为 .env 并填入实际值

# 服务端口（默认 3000）
PORT=3000

# 数据目录（本地文件模式使用，Postgres 模式可忽略）
DATA_DIR=./data
UPLOAD_DIR=./uploads

# Postgres 数据库连接字符串（配置后自动使用 Postgres 存储）
# Neon 免费数据库：https://neon.tech
# 格式：postgresql://用户名:密码@主机:5432/数据库名?sslmode=require
DATABASE_URL=
