# 课表管理平台 开发文档

> 本文档记录项目从开发至今的所有需求变更与实现细节，后续每次修改均需追加记录。

---

## 项目概述

课表管理平台是一个基于 Node.js + Express 的中小学课表管理系统，支持课表上传解析、班级/教师课表查看、课时统计、冲突分析、课表调整（对调）、Excel 导出等功能。

> 项目启动日期：2026 年 9 月 2 日

### 技术栈
- **后端**：Node.js + Express + xlsx (SheetJS) + ExcelJS
- **前端**：原生 HTML/CSS/JavaScript（无框架）
- **数据存储**：本地 JSON 文件（`data/` 目录）

### 目录结构
```
├── server.js              # 后端服务入口与 API 路由
├── utils/
│   ├── parser.js          # Excel 课表解析（总课表/教师安排/会议/调休）
│   ├── analyzer.js        # 冲突分析（教师/班级/教室/会议/调休）
│   ├── statistics.js      # 课时统计（教师/班级）
│   ├── exporter.js        # Excel 导出（班级课表/教师课表/课时统计）
│   ├── swap.js            # 课表对调核心算法（含关联科目冲突检查）
│   └── workbookWriter.js  # Excel 工作簿同步修改与调课记录管理
├── public/
│   ├── index.html         # 前端页面
│   ├── css/style.css      # 样式
│   └── js/app.js          # 前端逻辑
└── data/                  # 上传的课表数据与模板
```

---

## 变更记录

### 2026-09-07 | 课表管理平台完整实现（初始版本）

**提交**：`dec535f`

**说明**：项目于 2026-09-02 启动，9 月 2 日至 7 日期间完成初始开发，9 月 7 日首次提交。

**需求**：从零搭建课表管理平台，实现课表上传解析、班级/教师课表查看、课时统计、冲突分析、Excel 导出等核心功能。

**修改文件**：`server.js`、`utils/parser.js`、`utils/analyzer.js`、`utils/statistics.js`、`utils/exporter.js`、`public/index.html`、`public/css/style.css`、`public/js/app.js`、`package.json` 等全部初始文件

**实现内容**：
1. **上传解析**：支持高中总课表格式（单/双周总课表 + 教师安排 + 会议时间工作表）
2. **班级/教师课表**：可搜索选择框（combobox，支持拼音搜索）、完整节次显示、班级任课教师配置表
3. **课时统计**：按早自习/白课/晚自习分类统计，支持单双周切换
4. **冲突分析**：教师/班级/教室/会议四类冲突检测，单双周标注，按教师分组展示
5. **教师课表高亮**：重课冲突（红色）、会议冲突（橙色）、会议水印（浅黄色）
6. **UI**：悬停高亮、圆角头像、响应式布局
7. **Excel 导出**：班级课表、教师课表、教师课时统计导出

---

### 2026-09-08 | 移除过期会议课表文件

**提交**：`986e98c`

**需求**：清理仓库中过期的 20260830 会议课表 Excel 示例文件。

**修改文件**：删除 `高中总课表20260830.xlsx`

---

### 2026-09-10 | feat/teacher-swap 分支合并

**需求来源**：将 `feat/teacher-swap` 分支合并到 `main`，新增两大核心能力。

**实现内容**：

1. **课表调整（同班课程对调）功能**
   - 新增 `utils/swap.js`：核心算法模块
     - `computeSwapCandidates`：计算同班级内所有可对调候选课程
     - `checkSwappable`：四项校验——时段类型相同、双方教师在对方时段空闲、双方无会议冲突
     - `executeSwap`：交换两节课的 subject 与 teacher（保留 class/weekday/period）
   - 后端新增 3 个 API：`GET /api/swap/candidates`、`POST /api/swap/execute`、`POST /api/swap/undo`
   - 前端新增「课表调整」视图：教师课表/班级课表双模式、主网格 + 双参考课表、红/蓝/绿三色状态高亮、右键查看禁选原因、撤销功能

2. **教师调休冲突检测**
   - `utils/parser.js` 新增 `parseLeaveSheet`：支持三种调休表格式（列格式/网格格式/列表格式）
   - `utils/analyzer.js` 新增第 5 类冲突「调休冲突」（仅单周有效）
   - 教师课表视图与冲突分析中以紫色高亮调休冲突

3. **导出模块重写**
   - `utils/exporter.js`：班级课表导出增加任课教师配置表；教师课表导出增加会议水印与冲突配色（重课红/会议橙/调休紫）；统一显示周一至周日与完整节次行

4. **其他优化**
   - 静态资源设置 `no-store` 禁用缓存
   - 上传文件保存为模板供下载接口优先返回
   - 教师列表合并教师安排表中的教师，避免遗漏
   - 会议冲突检测修复为仅检查当前教师自身课程

---

### 2026-09-10 | 课时统计导出列缺失修复

**需求**：教师课时统计导出的 Excel 只有 7 列，而网页表格有 11 列，缺少「早自习」「白课」「晚自习」「每日分布」。

**修改文件**：`utils/exporter.js`

**实现**：
- `exportTeacherStatistics` 表头从 7 列扩展为 11 列：教师 | 早自习 | 白课 | 晚自习 | 周课时 | 月课时(约) | 班级数 | 科目数 | 每日分布 | 班级 | 科目
- 数据行补充 `morningHours`、`dayHours`、`eveningHours`，`dailyDistribution` 按周一至周日用 `/` 拼接
- 列宽同步调整

---

### 2026-09-10 | 教师课时统计新增「自习」列

**需求**：在「晚自习」后增加「自习」列，统计某教师的自习次数；不管自习在白课时段还是晚自习时段都属于自习，不计入白课或晚自习。

**修改文件**：`utils/statistics.js`、`public/js/app.js`、`utils/exporter.js`

**实现**：
- `statistics.js` 新增 `isSelfStudySubject` 判断（匹配含"自习"的科目）；重写 `teacherStatistics` 时段分类逻辑，每个时段若含自习科目则整节计入 `selfStudyHours`，不再归入早自习/白课/晚自习
- `app.js` 教师课时统计表在「晚自习」后新增「自习」列
- `exporter.js` 导出同步新增「自习」列，列数 11 → 12

---

### 2026-09-10 | 教师课时统计新增「班会」列

**需求**：在「晚自习」后增加「班会」列，统计某教师的班会次数；不管班会在白课时段还是晚自习时段都属于班会，不计入白课或晚自习。

**修改文件**：`utils/statistics.js`、`public/js/app.js`、`utils/exporter.js`

**实现**：
- `statistics.js` 新增 `isClassMeetingSubject` 判断（匹配含"班会"的科目）；时段分类优先级：班会 > 自习 > 时段类型；新增 `classMeetingHours` 字段
- `app.js` 教师课时统计表列顺序：早自习 | 白课 | 晚自习 | **班会** | 自习 | 周课时...
- `exporter.js` 导出同步，列数 12 → 13

---

### 2026-09-10 | 课时统计界面增加筛选框

**需求**：在课时统计界面，周次切换位置后面添加一个筛选输入框。切换到教师课时统计时筛选教师，切换到班级课时统计时筛选班级；筛选框样式与教师课表/班级课表的筛选框一致。

**修改文件**：`public/index.html`、`public/js/app.js`

**实现**：
- `index.html` 课时统计 `filter-bar` 内新增两个 combobox：`stats-teacher-select`（教师，默认显示）、`stats-class-select`（班级，默认隐藏）
- `app.js`：
  - 新增 `initStatsFilter()`：首次加载时用 `/api/teachers` 和 `/api/classes` 初始化两个 combobox（复用 `groupTeachers`/`groupClasses` 分组）
  - 新增 `updateStatsFilterVisibility()`：根据当前统计 tab 切换显示教师或班级筛选框
  - `loadTeacherStats(filter)` / `loadClassStats(filter)` 接收筛选参数做精确匹配过滤，无匹配时显示提示
  - tab 切换时更新 `activeStatsTab` 并切换筛选框可见性

---

### 2026-09-10 | 筛选框为空时展示全部数据

**需求**：当教师筛选框或班级筛选框为空时，展示全部教师或班级的课时统计。

**修改文件**：`public/js/app.js`

**实现**：
- 在 `initStatsFilter` 中为两个筛选 input 追加 `input` 事件监听
- 当输入框内容被清空（`!value.trim()`）时，清除 combobox 内部 `_value`、重置筛选状态变量，并调用 `loadTeacherStats('')` / `loadClassStats('')` 重新加载全部数据
- `loadTeacherStats`/`loadClassStats` 本身已有空值判断，传入空字符串即展示全部

---

### 2026-09-11 | 班级课时统计导出与课表模板重写

**提交**：`0a7bc9f`

**需求**：新增班级课时统计导出功能；教师课时统计导出支持按单个教师筛选；课表默认模板重写为匹配真实模版格式；导出按钮改为动态绑定并携带周次参数；课时统计筛选框增加"全部"选项。

**修改文件**：`server.js`、`utils/exporter.js`、`public/index.html`、`public/js/app.js`

**实现**：
1. **班级课时统计导出**
   - `utils/exporter.js` 新增 `exportClassStatistics`：导出班级课时统计 Excel（班级 | 早自习 | 白课 | 晚自习 | 周课时），支持筛选单个班级
   - `server.js` 新增 `GET /api/export/class-stats`，支持 `?class=` 参数按单个班级筛选
2. **教师课时统计导出筛选**
   - `server.js` 的 `GET /api/export/teacher-stats` 新增 `?teacher=` 参数，支持按单个教师筛选导出；文件名随筛选教师变化
3. **课表模板重写**
   - `server.js` 默认模板生成完全重写，匹配真实课表模版格式：
     - 工作表名称改为带数字前缀：`1单周总课表`、`2双周总课表`、`3教师安排`、`4会议时间`、`5教师调休`、`6作息时间`、`7调课记录`
     - 节次标签从 `早/1/2/...` 改为 `早自习/第1节/第2节/...`
     - 示例数据更新为真实班级代号（2401班、2402班）和教师姓名
     - 新增会议时间表（含教研组会议示例）、教师调休表（含示例数据）、作息时间表（空表）、调课记录表（含表头与合并单元格）
     - 删除旧的"格式说明"工作表，改为直接在模板中体现格式
     - 日期行、节次行、会议名称等使用合并单元格与统一样式
4. **导出按钮动态绑定**
   - `public/index.html` 导出按钮从内联 `onclick` 改为 `id` 绑定方式
   - `public/js/app.js` 概览页导出按钮通过 `exportUrl()` 携带当前周次参数；按钮文字显示当前周次
   - 课时统计导出按钮文字根据当前 tab 和筛选选择动态更新（如"导出 全部教师课时统计"或"导出 刘婷 课时统计"）
5. **课时统计筛选框增加"全部"选项**
   - `public/js/app.js` 教师/班级筛选 combobox 列表首项增加"全部教师"/"全部班级"，选择时清除筛选展示全部数据
   - 新增 `groupTeachersWithAll`、`groupClassesWithAll` 分组函数，将"全部"选项置顶显示

---

### 2026-09-13 | 调课工作簿持久化与关联科目同步

**需求**：上传的课表工作簿需保存在系统中，调课时同步修改 Excel 工作簿中对应课程单元格，并在"7调课记录"工作表追加记录；高一高二单双周关联科目（信息↔心理、美术↔音乐）需同步修改；上传时解析已有调课记录展示在调课界面。

**修改文件**：`utils/workbookWriter.js`（新增）、`server.js`、`utils/swap.js`

**实现**：

1. **新增 `utils/workbookWriter.js`**
   - `findCellPosition`：在 Excel 工作表中定位指定班级+星期+节次的单元格（处理合并单元格去重）
   - `modifyWorkbookOnSwap`：调课时同步修改 Excel 单元格值
     - 高三：只修改当前周次工作表
     - 高一/高二：修改当前周次工作表 + 对周关联科目工作表
   - `ensureSwapRecordSheet`：确保"7调课记录"工作表存在（10列格式）
   - `appendSwapRecord`：追加调课记录，序号自动递增
   - `getSwapRecords`：读取已有调课记录
   - `getGradeLevel`：使用当前年份动态计算年级（高三只改单周/双周）

2. **调课记录格式**（10列）：
   - 序号 | 单双周 | 班级 | 调出课程(节次/科目/教师) | 调入课程(节次/科目/教师) | 变动日期
   - 班级列共用（调出和调入为同班）

3. **server.js 调课流程**
   - `POST /api/swap/execute`：执行 JSON 调换 → 同步对周 JSON（高一/高二）→ 修改 Excel → 追加调课记录
   - `GET /api/swap/records`：返回 Excel 中的调课记录
   - 调课记录格式示例：`2401班：单周周一第2节语文（姚绍兰）与单周周二第5节化学（杨良英）互换2026-09-10-15:09:56`

4. **关联科目同步逻辑**
   - 关联映射：信息(单周)↔心理(双周)、美术(单周)↔音乐(双周)
   - JSON 层：高一/高二调课时自动在对周执行同样交换
   - Excel 层：高一/高二调课时修改当前周工作表 + 关联科目时修改对周工作表

---

### 2026-09-13 | 调课冲突检查增强：对周教师冲突分析

**需求**：高一/高二调课时对周也会同步修改，任何调课都需执行对周冲突分析，确保对周涉及的教师都没有冲突。

**修改文件**：`utils/swap.js`、`server.js`

**实现**：

1. **`checkSwappable` 新增第5项检查：对周教师冲突检查**
   - 不再限于关联科目，所有高一/高二调课都检查对周冲突
   - 检查对周源位置教师是否在目标时段（对周）有其他课
   - 检查对周目标位置教师是否在源时段（对周）有其他课
   - 关联科目（信息↔心理、美术↔音乐）在拒绝原因中标注关联关系

2. **`computeSwapCandidates` 接收全量 entries**
   - 新增 `allEntries` 参数（含所有周次数据）
   - server.js 候选 API 传入 `data.entries` 全量数据

3. **冲突检查优先级**（5层）
   - 第1层：时段类型相同（早自习/白课/晚自习）
   - 第2层：源教师在目标时段空闲（当前周）
   - 第3层：目标教师在源时段空闲（当前周）
   - 第4层：双方无会议冲突
   - 第5层：对周教师冲突检查（源/目标位置对周教师在目标/源时段空闲）

---

### 2026-09-13 | 账号管理系统（需求规划，尚未实现）

**分支**：`feat/account-management`

**需求**：增加多用户账号管理系统，实现不同角色的权限分级和数据隔离。

**用户角色与权限**：

| 角色 | 数量 | 登录方式 | 权限范围 |
|------|------|----------|----------|
| 超级管理员 | 仅 1 个账号，支持多个登录密码 | 登录 | 全部权限：上传课表、调课、查看自己的课表、导出、冲突分析、分配管理员账号 |
| 管理员 | 多个 | 登录（账号由超管分配） | 除账号管理外的全部权限：上传课表、调课、查看自己的课表、导出、冲突分析 |
| 教师 | 无需账号 | 不需登录，直接访问 | 除上传课表和调课外的全部操作：查看课表、课时统计、冲突分析、导出、查看调课候选 |

**数据隔离规则**：

- 超级管理员和各管理员各自管理**自己上传的课表**，相互独立、互不可见
- 每个管理员上传的课表独立存储、独立操作（调课/导出等均针对自己的课表）
- 教师（未登录用户）看到的是**超级管理员上传的课表**
- 超管删除某管理员账号时，该管理员的课表数据**一并删除**

**界面交互**：

1. **登录入口**：在课表上传界面右上角放置"登录"按钮
2. **登录后显示**：按钮文字根据用户类型变为"超级管理员"或"管理员"
3. **退出登录**：已登录状态下再次点击身份按钮，弹出登出确认弹窗（取消/确认），防止误操作登出
4. **账号管理入口**：仅超级管理员登录后，左侧边栏增加"管理账户"标签页
5. **账号管理界面**：超级管理员在该界面中增删管理员账号、设置/重置密码
6. **密码修改**：管理员不能自行修改密码，仅超管可重置任意管理员密码

**初始账号与密码恢复**：

- 超级管理员账号：`17347363572`（固定不变）
- 超管支持多个登录密码，其中密码 `990322` 为**永久有效密码**，不可删除、不可修改，始终可用于登录
- 密码 `990322` 在系统中以哈希加密存储，不存明文
- 超管可在账号管理界面中新增/删除其他登录密码
- 超管忘记密码：提供两种重置方式（双重保障）
  - 方式一：命令行脚本 `node reset-superadmin.js`，运行后重置为默认密码 `990322`，适用于有服务器 SSH 权限的场景
  - 方式二：隐藏 API 接口 `/api/reset-super`，需回答安全验证问题（如注册时设置的密保问题），适用于无 SSH 权限的 Web 部署场景

**异常与边界处理**：

- 教师访问时超管未上传课表：显示友好提示"暂无课表"
- 登录态过期后执行操作：直接弹出"登录已过期，请重新登录"提示
- 未登录用户点击上传：弹出"无权限，请先登录"提示
- 未登录用户提交调课：弹出"无权限，请先登录"提示（但可查看调课候选）

**待实现要点**：

- 用户数据存储：`data/users.json`（沿用项目 JSON 文件存储方式）
- 密码加密：后端存储哈希值，不存明文
- Session 机制：登录后保持会话，设置过期时间
- 课表数据隔离：上传时关联用户 ID，查询时按用户 ID 过滤
- 超管密码重置脚本：`reset-superadmin.js`
- 前端权限控制：
  - 上传课表入口：**不隐藏**，未登录时点击上传弹出"无权限"提示
  - 调课功能：**不隐藏**，未登录可查看候选但不能提交
  - 教师（未登录）可正常使用：查看课表、课时统计、冲突分析、导出、查看调课候选

---

### 2026-09-14 | 上传文件名乱码修复

**需求**：上传课表后，"课表信息"中显示的文件名出现乱码（如"è¯¾è¡¨å¯¼å...xlsx"），实际应为"课表导入模版.xlsx"。

**修改文件**：`server.js`

**实现**：

1. **原因**：multer 默认按 latin1 编码解码中文文件名，而保存到 `data.json` 时直接用了 `req.file.originalname`（latin1编码的乱码字符串），未经过 `fixFilename()` 处理
2. **代码修复**：`server.js` 保存文件名处改为 `filename: fixFilename(req.file.originalname)`，确保新上传的文件名正确解码（latin1→UTF-8）后再存储
3. **历史数据修复**：对已有的 `data/schedule_super.json` 和 `data/schedule.json` 中存储的乱码文件名执行相同的解码修复
4. **`fixFilename()` 函数**（server.js 第73-88行）：先尝试 `Buffer.from(name, 'latin1').toString('utf8')`，若仍含乱码则尝试 GBK 编码兜底

---

### 2026-09-14 | 筛选栏控件高度统一与样式优化

**需求**：各页面筛选栏中的下拉选择框（周次、课表类型、教师/班级选择器）与按钮高度不一致，尤其在 Safari 浏览器中差异明显，需统一高度并保持同一水平线对齐，浏览器缩放时自动适应。

**修改文件**：`public/css/style.css`

**实现**：

1. **统一控件高度为 36px**
   - `.week-bar select`、`.filter-bar select`、`.swap-mode-select`、`.combobox-input-wrap`、`.btn` 均设为 `height: 36px` + `box-sizing: border-box`
   - `.week-bar`、`.filter-group` 容器也设为 `height: 36px`

2. **Safari 兼容**
   - 添加 `-webkit-appearance: none` + `appearance: none` 移除 Safari 原生 select 样式
   - 用 SVG 背景图替代原生下拉箭头

3. **选择器宽度调窄**
   - 周次选择器：`min-width: 80px; max-width: 120px`
   - 课表类型/年级班级选择器：`min-width: 100px; max-width: 140px`
   - 教师/班级 combobox：`width: clamp(100px, 12vw, 150px)`

4. **撤销按钮位置调整**
   - 撤销按钮移至"执行调整"按钮后面，两个按钮紧挨在同一行

5. **flex 布局对齐**
   - `.filter-bar` 已有 `align-items: center` + `flex-wrap: wrap`，保证所有元素同一水平线且缩放时自动换行

---

### 2026-09-14 | 调课网格样式修复：行高固定与圆角

**需求**：调课模式下选中一节课后课表变大；主调课网格四个角是直角，与右侧参考课表的圆角不一致。

**修改文件**：`public/css/style.css`

**实现**：

1. **调课网格行高固定**
   - `.swap-grid-wrap` 设置 `--row-h: 32px`，覆盖全局行高变量
   - `.swap-grid-wrap .schedule-grid td` 设置 `height: 32px !important`，防止班级课表大行高泄漏
   - `th`/`td` 的 padding、font-size 均加 `!important` 确保覆盖

2. **主调课网格圆角**
   - `.swap-grid-wrap` 添加 `border-radius: 8px`、`box-shadow`、`background`
   - `overflow` 改为 `hidden`，裁切内部表格直角

---

### 2026-09-14 | 重课单元格左右对半分割显示与独立调课

**需求**：当教师在同一时间段被安排了两个班的课（重课）时，当前系统将两个班名拼在一个单元格中显示（如"2405班 / 2601班"），不直观且调课时无法单独操作其中一节课。需改为左右对半分割显示，并支持独立调课。

**修改文件**：`public/js/app.js`、`public/css/style.css`、`utils/swap.js`

**实现**：

1. **重课单元格左右对半分割显示**
   - 当同一时段存在两节课（重课）时，单元格从中间竖向分为左右两半
   - 左半显示第一节课（科目 + 班级），右半显示第二节课（科目 + 班级）
   - 两半都保持高亮（红色边框 + 浅红背景）
   - 教师课表视图（`renderSchedule`）和调课视图（`renderSwapGrid`）均需生效

2. **调课独立选择**
   - 调课模式下，左右两半各自独立可选
   - 选中左半时，以该班那节课为源课程计算可换目标
   - 选中右半时，以该班那节课为源课程计算可换目标
   - `swapState.source` 需记录具体是哪一节课（含班级信息），而非整个单元格

3. **调走一节后恢复整格显示**
   - 当其中一节课被调走，该单元格不再左右对半分割
   - 恢复为正常的整格单课显示（只剩一节课）

4. **适用范围**
   - 教师课表视图：重课单元格左右对半分割
   - 调课视图：重课单元格左右对半分割 + 独立选择
   - 班级课表视图：无重课（同一班级同一时段不会有两节课），不受影响

---

### 2026-09-17 | 调课冲突检查修复：对周同步跳过重复校验

**需求**：调课时对周同步交换导致误判冲突。例如 2601班双周周三第8节数学与第2节语文互换，双周测试通过但整体调课失败，错误"教师「樊启云」在周三第8节（双周）已有其他课程"。

**修改文件**：`utils/swap.js`、`server.js`

**实现**：

1. **`executeSwap` 新增 `skipCheck` 参数**
   - 函数签名：`executeSwap(entries, source, target, weekType, meetings, teacherMap, skipCheck)`
   - `skipCheck=true` 时跳过 `checkSwappable` 校验
   - 用于对周同步交换（第一次调用已做过完整校验）

2. **server.js 对周同步跳过校验**
   - 第一次调用：`executeSwap(data.entries, source, target, weekType, ...)` — 完整校验
   - 第二次调用：`executeSwap(data.entries, otherSource, otherTarget, otherWeekType, ..., true)` — 跳过校验
   - 原因：第一次交换后 `data.entries` 已被修改，对周检查看到的是"交换后的双周状态"，会误判原本就存在的重课为"新增冲突"

3. **`checkSwappable` 同周检查排除条件修正**
   - 路径1（源教师在目标时段是否有其他课）：排除 target 位置自身（原本排除 source 位置，无效）
   - 路径2（目标教师在源时段是否有其他课）：排除 source 位置自身（原本排除 target 位置，无效）
   - 当 `source.teacher === target.teacher` 时跳过路径1和路径2（同教师互换不新增重课）

4. **`checkSwappable` 对周检查修正**
   - `srcOtherBusy` 排除对周 target 位置自身 + 对周 source 位置自身
   - `tgtOtherBusy` 排除对周 source 位置自身 + 对周 target 位置自身
   - 新增 `sameTeacherInOtherWeek` 判断：当对周 source 和 target 位置原教师是同一人时跳过检查（调课后对周无变化）

---

### 2026-09-17 | 调课撤销时同步删除调课记录

**需求**：调课撤销时，Excel"7调课记录"工作表中对应的调课记录也要删除，保持数据一致。

**修改文件**：`utils/workbookWriter.js`、`server.js`、`public/js/app.js`

**实现**：

1. **`workbookWriter.js` 新增 `removeLastSwapRecord`**
   - 查找"7调课记录"工作表中最后一行有数据的行（表头占2行，从第3行开始）
   - 清空该行所有10列的数据
   - 保存工作簿

2. **server.js 撤销接口增强**
   - `_lastBackup` 新增存储 `source` 和 `target` 信息（用于恢复 Excel 单元格）
   - `POST /api/swap/undo` 改为 `async`：
     - 恢复 `data.entries`（JSON 数据备份）
     - 再交换一次 Excel 单元格（`modifyWorkbookOnSwap` 是对称操作，恢复原始值）
     - 高一/高二对周也恢复
     - 调用 `removeLastSwapRecord` 删除最后一条调课记录

3. **app.js 前端刷新调课记录**
   - 撤销成功后调用 `loadSwapRecords()` 刷新调课记录列表
   - 之前只刷新了调课网格，调课记录列表未更新

---

### 2026-09-17 | 总览页面统计数据一致性修复

**需求**：上传成功提示与总览页面数据显示不一致。上传提示教师99名/科目18种，总览页面显示教师95名/科目16种。

**修改文件**：`server.js`

**实现**：

1. **问题原因**
   - `/api/overview` 使用按周次过滤后的 entries 统计教师和科目数
   - 只在双周有课的教师/科目在单周视图中被漏掉
   - 上传时使用全量 entries 统计，数据完整

2. **修复方案**
   - `totalEntries` 按周次统计（单周2020条）
   - `classCount`/`teacherCount`/`subjectCount` 基于全量 entries 统计
   - 两个 `getStats` 调用：`weekStats`（过滤后，用于 totalEntries）和 `allStats`（全量，用于教师/科目/班级数）

---

## 后续修改记录模板

每次修改请在下方追加，格式如下：

```
### YYYY-MM-DD | 简要标题

**需求**：用户提出的需求描述。

**修改文件**：列出涉及的文件路径

**实现**：具体实现说明
```
